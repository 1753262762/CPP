#include<bits/stdc++.h>
using namespace std;
int main()
{
	/*动态数组*/ 
	vector<int> vec;//定义 
	vec.push_bank(1);//插入
	vec.pop_back();//删除 
	vec.front();//第一个 
	vec.back();//最后一个 
	vec[0];//随机 
	vec.size();//大小 
	vector<int>().swap(vac);//清内存 
	/*栈*/ 
	stack<int>s;
	s.push(1);//压入 
	s.top();//栈顶 
	s.pop();//弹出 
	s.size();//大小 
	s.empty();//栈空 
	return 0;
}
